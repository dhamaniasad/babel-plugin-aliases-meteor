{
  "author": "",
  "bundleDependencies": false,
  "deprecated": false,
  "description": "A babel plugin to transform aliases for Meteor",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0",
    "eslint": "^3.10.2",
    "eslint-config-airbnb-base": "^10.0.1",
    "eslint-plugin-import": "^2.2.0"
  },
  "license": "ISC",
  "main": "dist/index.js",
  "name": "babel-plugin-meteoralias",
  "scripts": {
    "compile": "babel src -d dist",
    "lint": "eslint src/",
    "prepublish": "npm run lint && npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "1.0.6",
  "repository": "github:dhamaniasad/babel-plugin-aliases-meteor"
}
